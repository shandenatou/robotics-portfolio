# 发布清单

[返回首页](../README.md)

本仓库已于 2026-09-07 根据账号所有者的明确授权改为公开。以下清单保留独立审阅状态，公开授权不等于版权、归属等事项已全部核验。不要从整个求职工作区直接执行 `git add .`，上传根目录应仅限本仓库。

## 内容确认

- [x] 账号所有者已明确授权公开当前仓库内容。
- [ ] 三个主题的个人负责模块与协作范围已确认。
- [ ] 公司/合作项目的文字、指标、代码、图片、视频分别获准公开。
- [ ] 引用、上游版本、版权与许可证已核验。
- [ ] 报告中的历史运行没有被全部归为个人成果。
- [ ] FastWAM 各设置/检查点口径已确认，原始聚合结果已追溯。
- [ ] LingBot 的 success-demo-only 版本与完整方案分开。
- [ ] 记忆的结构、配置、checkpoint 和指标已逐一绑定。
- [ ] 无手机号、微信、私有邮箱、服务器地址、内部绝对路径、密钥或内部平台链接。
- [ ] 没有模型权重、原始训练日志、数据集、备份包或未经许可的媒体。

## 后续内容发布检查

1. 核对 `portfolio.json` 中的四个确认字段。只有对应人工审阅完成后才改为 `confirmed`，同时将 `release_status` 改为 `ready`。通过机器检查本身不构成公开授权。
2. 审阅并更新各页的草稿说明和复现状态，移除不允许公开的条目。
3. 运行普通检查、单元测试和 `--release` 检查。
4. 新增内容经确认后再上传；不要扩大到仓库外的原始材料。
5. 发布后核对 README、相对链接、附件与权限，再将仓库链接加入简历。

不应为了通过检查而填入未经核实的确认状态。文档检查工具只覆盖有限的结构、路径和敏感内容模式，不是完整的泄露检测器。
